{
  "name": "spitestack",
  "private": true,
  "workspaces": [
    "packages/*",
    "packages/spitedb/napi"
  ],
  "scripts": {
    "build": "bun run --filter '*' build",
    "test": "bun run --filter '*' test"
  }
}
